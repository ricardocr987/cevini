import MailForm from "../MailForm";

export const ContactSection: React.FC = () => {
    return (
        <section className="bg-gray-200 px-3 pb-3">
            <MailForm />
        </section>
    );
};

export default ContactSection;
